# ---- network ----
SEPOLIA_RPC_URL="https://ethereum-sepolia-rpc.publicnode.com"
SEPOLIA_PRIVATE_KEY="0x..."          # deployer / keeper (throwaway key)

# ---- deployed addresses (from `pnpm deploy:sepolia`) ----
BATCH_ROUTER="0x..."
CUSDC="0x..."
CWETH="0x..."
USDC="0x..."
WETH="0x..."

# ---- pool seeding (optional) ----
POOL_USDC="300000"                   # => 1 WETH = 3000 USDC
POOL_WETH="100"

# ---- keeper (optional) ----
SLIPPAGE_BPS="300"                   # 3%
# BATCH_ID="1"                       # defaults to the current batch

# ---- demo traders (optional) ----
TRADER_KEYS="0xkey1,0xkey2,0xkey3"   # each needs a little Sepolia ETH
TRADER_AMTS="128400,52900,310750"
